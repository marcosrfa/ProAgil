ENtity Framework
dotnet ef migrations add NOME_DA_MIGRATION -> cria o esquema de banco de dados no Código
dotnet ef database update -> Cria o arquivo de banco e dados

Adicionar campo no objeto evento
DROP na base: dotnet ef database drop
DROP na Migration:  dotnet ef migrations remove

{ [DECORATOR] }

*ng -> Diretivas estruturais do angular
{{ [Bind com interpolação] }}

Two Way DataBind é [()] => Caixa de Bananas

filtro por meio de pipe "|"

Toda vez que declara uma variável dentro de uma [(ngModel)], eu consigo reutilizar esse valor

GET / SET -> Encapsulamento

Observable comparado ao Promisses é LAZY, enquanto o Promisses é EGGER. 